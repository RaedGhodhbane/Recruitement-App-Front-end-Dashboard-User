import { RechercheMessagesPipe } from './recherche-messages.pipe';

describe('RechercheMessagesPipe', () => {
  it('create an instance', () => {
    const pipe = new RechercheMessagesPipe();
    expect(pipe).toBeTruthy();
  });
});
