import { RechercheCandidatiesByCandidatePipe } from './recherche-candidaties-by-candidate.pipe';

describe('RechercheCandidatiesByCandidatePipe', () => {
  it('create an instance', () => {
    const pipe = new RechercheCandidatiesByCandidatePipe();
    expect(pipe).toBeTruthy();
  });
});
