import { RechercheRecruitersPipe } from './recherche-recruiters.pipe';

describe('RechercheRecruitersPipe', () => {
  it('create an instance', () => {
    const pipe = new RechercheRecruitersPipe();
    expect(pipe).toBeTruthy();
  });
});
