import { RechercheAddressPipe } from './recherche-address.pipe';

describe('RechercheAddressPipe', () => {
  it('create an instance', () => {
    const pipe = new RechercheAddressPipe();
    expect(pipe).toBeTruthy();
  });
});
