import { RechercheCandidatesPipe } from './recherche-candidates.pipe';

describe('RechercheCandidatesPipe', () => {
  it('create an instance', () => {
    const pipe = new RechercheCandidatesPipe();
    expect(pipe).toBeTruthy();
  });
});
